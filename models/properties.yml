sources:
  - name: 'datalake'
    database: DATALAKE 
    schema: DATALAKE_ENG_RAWDATA
    tables:
      - name: EMPLOYEES
      - name: DEPARTMENTS

models:
  - name: 'employees'
    config:
      materialized: 'table'
  - name: 'ephemeral_test'
    config:
      materialized: 'ephemeral'