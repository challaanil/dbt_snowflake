sources:
  - name: DATALAKE
    database: DATALAKE_ENG_LANDING
    tables:
      - name: EMPLOYEES
      - name: DEPARTMENTS

models:
  - name: 'EMPLOYEES'
    config:
      materialized: 'table'
  - name: 'DEPARTMENTS'
    config:
      materialized: 'table'